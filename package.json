{
  "dependencies": {
    "firebase": "^10.7.1"
  },
  "scripts": {
    "start": "cd backend && uvicorn server:app --reload",
    "build": "cd front-end && npm i --legacy-peer-deps && npm run build"
  }
}
